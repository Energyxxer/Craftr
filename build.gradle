apply plugin: "idea"
apply plugin: "java"

group = craftrGroup
version = craftrVersion

repositories {
    mavenCentral()
}

dependencies {
    compile group: "org.jetbrains", name: "annotations", version: "15.0"
}