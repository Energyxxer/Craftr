package craftr.lang.entities;

import craftr.lang.entities.features.*;

/**
* Created by Energyxxer on 15/7/2017.
*/
public scoreHolder wolf extends living_base implements breedable, tameable, sittable {
    
    //Getters for NBT Data
    public native boolean isAngry();
    public native DyeColor getCollarColor();
    
    //Setters for NBT Data
    public native void setAngry(boolean angry);
    public native void setCollarColor(DyeColor color);
}
