package craftr.lang.entities;

/**
* Created by Energyxxer on 15/7/2017.
*/
public abstract entity projectile_base extends entity_base {
    
    //Getters for NBT Data
    public native int getXTile();
    public native int getYTile();
    public native int getZTile();
    //inTile tag currently unsupported
    
    //Setters for NBT Data
    public native void setXTile(int x);
    public native void setYTile(int y);
    public native void setZTile(int z);
    //inTile tag currently unsupported
}
