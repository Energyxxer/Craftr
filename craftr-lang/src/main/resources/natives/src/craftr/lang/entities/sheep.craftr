package craftr.lang.entities;

import craftr.lang.entities.features.*;

/**
* Created by Energyxxer on 15/7/2017.
*/
public scoreHolder sheep extends living_base implements breedable {
    
    //Getters for NBT Data
    public native boolean isSheared();
    public native DyeColor getColor();
    
    //Setters for NBT Data
    public native void setSheared(boolean sheared);
    public native void setColor(DyeColor color);
}
