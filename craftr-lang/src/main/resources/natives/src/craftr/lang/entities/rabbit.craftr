package craftr.lang.entities;

import craftr.lang.entities.features.*;

/**
* Created by Energyxxer on 15/7/2017.
*/
public scoreHolder rabbit extends living_base implements breedable {
    
    //Getters for NBT Data
    public native int getRabbitType();
    public native int getCarrotTicks();
    
    //Setters for NBT Data
    public native void setRabbitType(int type);
    public native void setCarrotTicks(int ticks);
}
