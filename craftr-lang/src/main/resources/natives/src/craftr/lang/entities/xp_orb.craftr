package craftr.lang.entities;

/**
* Created by Energyxxer on 15/7/2017.
*/
public scoreHolder xp_orb extends entity_base {
    
    //Getters for NBT Data
    public native int getAge();
    public native int getHealth();
    public native int getValue();
    
    //Setters for NBT Data
    public native int setAge(int age);
    public native int setHealth(int health);
    public native int setValue(int value);
}
